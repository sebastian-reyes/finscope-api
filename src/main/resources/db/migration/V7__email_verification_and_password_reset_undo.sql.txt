-- Reverso de V7__email_verification_and_password_reset.sql.
--
-- No lo aplica Flyway: se guarda como texto para poder deshacer el cambio a mano si hiciera
-- falta, igual que el resto de reversos del proyecto.
--
-- Se pierden los enlaces pendientes y el estado de verificacion de cada cuenta. Lo primero
-- no importa --- caducan solos ---; lo segundo si: al volver a aplicar V7 todas las cuentas
-- empezarian de nuevo sin verificar y tendrian que verificarse otra vez.

DROP TABLE IF EXISTS account_tokens;

ALTER TABLE users
    DROP COLUMN IF EXISTS email_verified;
