-- Reverso de V10__catalogue_icons.sql.
--
-- No lo aplica Flyway: se guarda como texto para poder deshacer el cambio a mano si hiciera
-- falta, igual que el resto de reversos del proyecto.
--
-- Se pierden los iconos elegidos. Las categorias y los tags vuelven a pintarse con el icono
-- que el cliente deduce de su nombre.

ALTER TABLE tags
    DROP CONSTRAINT IF EXISTS ck_tags_icon,
    DROP COLUMN IF EXISTS icon;

ALTER TABLE categories
    DROP CONSTRAINT IF EXISTS ck_categories_icon,
    DROP COLUMN IF EXISTS icon;
